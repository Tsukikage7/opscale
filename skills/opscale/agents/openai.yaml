interface:
  display_name: "Opscale"
  short_description: "Read-only operations analytics for AI agents"
  default_prompt: "Use $opscale to inspect the database schema, map the business question to a metric plan, and answer with read-only SQL."
